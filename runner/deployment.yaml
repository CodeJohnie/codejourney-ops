apiVersion: apps/v1
kind: Deployment
metadata:
  name: github-runner
  namespace: ci
spec:
  replicas: 1
  selector:
    matchLabels:
      app: github-runner
  template:
    metadata:
      labels:
        app: github-runner
    spec:
      serviceAccountName: github-runner
      # Pin to master node — containerd socket path is consistent here
      nodeSelector:
        kubernetes.io/hostname: rpi-master
      volumes:
        - name: containerd-sock
          hostPath:
            path: /run/k3s/containerd/containerd.sock
            type: Socket
      containers:
        - name: runner
          image: myoung34/github-runner:latest
          env:
            - name: RUNNER_SCOPE
              value: repo
            - name: REPO_URL
              value: https://github.com/CodeJohnie/codejourney-ops
            - name: RUNNER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: runner-secrets
                  key: RUNNER_TOKEN
            - name: RUNNER_NAME
              value: k3s-homelab
            - name: RUNNER_LABELS
              value: self-hosted,k3s
            - name: EPHEMERAL
              value: "false"
            - name: DISABLE_AUTO_UPDATE
              value: "true"
            - name: CONTAINERD_ADDRESS
              value: /run/k3s/containerd/containerd.sock
            - name: CONTAINERD_NAMESPACE
              value: k8s.io
          volumeMounts:
            - name: containerd-sock
              mountPath: /run/k3s/containerd/containerd.sock
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
